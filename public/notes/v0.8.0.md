# Notes de Mise à Jour — v0.8.0 Alpha 🛡️

Mise à jour axée sur la **sécurité** et la **stabilité** de l'application.

## 🔐 Sécurité — Identifiants Admin
- Les identifiants de connexion administrateur ne sont **plus visibles en clair** dans le code source.
- Les mots de passe sont désormais **hashés en SHA-256** et comparés de manière sécurisée.
- **Anti brute-force** : 3 tentatives max, puis verrouillage de 30 secondes.

## 🛡️ Intégrité des Sauvegardes
- Chaque sauvegarde `localStorage` est désormais **signée avec un checksum**.
- Mode de modification manuelle détecté → **reset automatique** des données corrompues.
- L'import de sauvegarde est également protégé.

## 💰 Protection de l'Économie
- Les fonctions `addCoins`, `addGems` et `addEventTokens` respectent des **plafonds de sécurité** :
    - Max **50 000 pièces** par transaction
    - Max **5 000 gemmes** par transaction
    - Max **1 000 jetons** par transaction
- Les montants négatifs ou non-entiers sont rejetés.

## 💬 Chat Sécurisé
- Les messages sont désormais **isolés par salle de jeu** (room-only broadcast).
- **Rate limiting** : Maximum 5 messages toutes les 3 secondes.
- Longueur maximale des messages : 200 caractères.

## 🎯 Validation Serveur
- **Anti-téléportation** : Distance de déplacement maximale vérifiée par le serveur.
- **Anti-speedhack tir** : Cadence de tir minimale imposée (100ms entre chaque tir).
- **Validation des dégâts** : Dégâts aberrants (négatifs ou > 500) rejetés.

## 🐛 Correction de Bugs
- **Fix critique** : La variable `won` n'était pas définie dans `triggerGameOver`, provoquant un crash systématique en fin de partie.
- **Synchronisation** : `package.json` reflète désormais la version `0.8.0`.

*La sécurité n'est pas une option, c'est une fondation.* 🔒
